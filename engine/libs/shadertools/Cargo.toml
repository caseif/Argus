[package]
name = "argus_shadertools"
version.workspace = true
edition.workspace = true

[lib]
name = "argus_shadertools"
crate-type = ["rlib"]

[dependencies]
glsl = { workspace = true }
glslang = "0.6.2"
libc = { workspace = true }

[features]
default = ["all"]
c_abi = []
all = ["c_abi"]
