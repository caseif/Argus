name = render_vulkan
type = dynamic
languages = C,CXX
engine_module_deps = core,wm,resman,render
engine_library_deps = lowlevel
required_packages = Vulkan
include_dirs = ${GEN_INC_DIR},${ROOT_PROJECT_DIR}/external/libs/Vulkan-Headers/include,${SPIRV_CROSS_INCLUDE_DIR}
linker_deps = ${Vulkan_LIBRARY},${SPIRV_CROSS_LIBRARY}
